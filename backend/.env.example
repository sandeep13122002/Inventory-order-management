# Backend-only environment variables (for running the API outside compose).
# Copy to .env and adjust. Never commit the real .env.

DATABASE_URL=postgresql+psycopg2://inventory:change_me@localhost:5432/inventory_db
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
LOW_STOCK_THRESHOLD=10
SEED_ON_STARTUP=true
